# MDS - Manufacturing Data System
# Läuft parallel zu task-app auf Port 81

services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: mds
      POSTGRES_USER: mds
      POSTGRES_PASSWORD: ${DB_PASSWORD:-mds_dev_password}
    volumes:
      - /srv/mds/postgres:/var/lib/postgresql/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U mds -d mds"]
      interval: 10s
      timeout: 5s
      retries: 5

  pgadmin:
    image: dpage/pgadmin4:latest
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_EMAIL:-admin@mds.local}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_PASSWORD:-admin}
      PGADMIN_CONFIG_SERVER_MODE: "False"
    ports:
      - "5050:80"
    volumes:
      - /srv/mds/pgadmin:/var/lib/pgadmin
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    environment:
      NODE_ENV: production
      PORT: 5000
      # Für node-pg-migrate
      DATABASE_URL: postgresql://mds:${DB_PASSWORD:-mds_dev_password}@db:5432/mds
      # Für Backend
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: mds
      DB_USER: mds
      DB_PASSWORD: ${DB_PASSWORD:-mds_dev_password}
      JWT_SECRET: ${JWT_SECRET:-change-this-in-production}
      JWT_EXPIRES_IN: 24h
      UPLOAD_PATH: /app/uploads
      MAX_FILE_SIZE: 52428800
      CORS_ORIGIN: "*"
    volumes:
      - /srv/mds/uploads:/app/uploads
      - /srv/mds/backups:/app/backups:ro
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped

  caddy:
    image: caddy:alpine
    ports:
      - "81:80"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - ./frontend/dist:/srv/frontend:ro
    depends_on:
      - backend
    restart: unless-stopped
