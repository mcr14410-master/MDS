# MDS Production Environment
# Kopiere diese Datei nach .env auf dem Pi

# ============================================================
# WICHTIG: Passwörter ändern vor dem ersten Start!
# ============================================================

# PostgreSQL
DB_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# JWT Secret (mindestens 32 Zeichen, zufällig generieren)
# Generieren mit: openssl rand -base64 32
JWT_SECRET=CHANGE_ME_RANDOM_SECRET_KEY_32_CHARS

# ============================================================
# Optionale Einstellungen (Defaults sind meist OK)
# ============================================================

# Node Environment
NODE_ENV=production

# Server Port (intern im Container)
PORT=5000

# JWT Token Gültigkeit
JWT_EXPIRES_IN=24h

# Max Upload Size (50MB)
MAX_FILE_SIZE=52428800
